<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= render @user %>
    <%= image_tag @user.avatar.url(:large) %>
      </h1>
    </section>
  </aside>
</div>